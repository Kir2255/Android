<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.labs">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".lab4.SignOutActivity"></activity>
        <activity android:name=".lab8.EightsLabActivity" />
        <activity android:name=".lab7.SevensLabActivity" />
        <activity android:name=".lab6.SixLabActivity" />
        <activity android:name=".lab5.FifthLabActivity" />
        <activity android:name=".lab4.SignInActivity" />
        <activity android:name=".lab2.SecondLabActivity" />
        <activity android:name=".lab1.FirstLabActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>